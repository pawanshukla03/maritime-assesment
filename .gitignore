# Embedding cache and cloned PDF repos (local data)
backend/data/

# Local env with secrets (copy from backend/.env.example to backend/.env)
backend/.env

# Launcher-generated batch file (paths with spaces)
_start_backend.bat
